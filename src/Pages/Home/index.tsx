import React from 'react';
import { Container } from './styled';

const Home: React.FC = () => {
    return (
        <Container>
            <h1>Bem vindo!</h1>
        </Container>
    );
};

export default Home;
